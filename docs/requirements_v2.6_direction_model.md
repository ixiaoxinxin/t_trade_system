# v2.6 次日涨跌方向分类模型需求文档

## 1. 目标

v2.6 训练第一个机器学习方向模型，在现有规则评分之外，输出：

- 次日上涨概率。
- 方向置信度。
- 模型方向：上涨 / 下跌或震荡。
- 模型版本、训练区间和评估指标。

本版本只做“方向判断”，不替代规则等级，不直接自动改交易建议。

## 2. 前置能力

v2.6 依赖 v2.5 已完成的数据地基：

- SQLite 样本库：`data/dataset/trade_dataset.sqlite3`。
- 样本主表：`dataset_samples`。
- 特征快照表：`feature_snapshot`。
- 标签表：`label_snapshot`。
- 时间序列切分：`data/dataset/splits/latest.json`。
- 真实交易记录：`trade_records`。

## 3. 固定持仓样本池

本版本新增固定持仓样本池，每天固定刷新并置顶展示：

| 股票代码 | 股票名称 | 用途 |
|---|---|---|
| 002192 | 融捷股份 | 固定持仓跟踪与训练样本沉淀 |
| 600096 | 云天化 | 固定持仓跟踪与训练样本沉淀 |
| 002213 | 大为股份 | 固定持仓跟踪与训练样本沉淀 |
| 000933 | 神火股份 | 固定持仓跟踪与训练样本沉淀 |
| 002466 | 天齐锂业 | 固定持仓跟踪与训练样本沉淀 |

### 3.1 展示规则

- 在卖点信号中固定置顶。
- 在午盘验证中固定置顶。
- 在次日复盘中固定置顶。
- 在页面「固定持仓」独立 Tab 一票一行展示买点、卖点、午盘、次日复盘和模型预测。
- 提供单独按钮刷新固定持仓买点和卖点，输出 `output/fixed_holdings_signals.csv`。

### 3.2 数据规则

- 固定持仓优先刷新日线和 1 分钟数据。
- 有行情时正常计算卖点、午盘结构和次日复盘。
- 行情为空时不隐藏股票，而是显示 `待刷新` 或 `行情待刷新`。
- `行情待刷新` 的次日复盘行不进入有效训练标签，避免污染模型。

## 4. 标签定义

v2.6 主标签为：

```text
direction_up_close = 次日收盘涨幅 > 0
```

含义：

- `1`：次日收盘价高于预测日参考价。
- `0`：次日收盘价不高于预测日参考价。

后续 v2.7 再训练 +1%、+2%、先止损等更贴近隔日T执行的概率模型。

## 5. 特征输入

模型读取 SQLite 的 `feature_snapshot`，优先使用已经沉淀的结构化特征：

- 日线收益率：1/3/5/20 日。
- 均线动量：MA5/10/20 缺口和斜率。
- 波动：ATR、历史波动率、近5日振幅。
- 成交：成交额、5日平均成交额、量比。
- K线：实体、上下影线、跳空。
- 分时：开盘强度、前15分钟收益和回撤、VWAP 偏离、路径标签。
- 市场环境：市场分层、风险等级。
- 板块：板块名称、板块状态、板块排名、广度、龙头强度。
- 规则结果：候选评分、尾盘评分、最终评分、隔夜等级。

## 6. 模型训练

### 6.1 算法

- 默认算法：LightGBM 二分类模型。
- 样本不足、标签单一或 LightGBM 不可用时，降级为正样本率基准概率，保证流程不中断。

### 6.2 切分

优先使用 `data/dataset/splits/latest.json`：

- train。
- validation。
- test。

如果切分文件不存在，则按 `predict_date` 顺序做 70% / 15% / 15% 切分，不随机打乱。

### 6.3 输出文件

- 模型文件：`data/models/direction_model_v2.6.joblib`。
- 特征列文件：`data/models/direction_model_v2.6_features.json`。
- 评估报告：`output/model_evaluation_v2.6.md`。
- 预测结果：`output/model_predictions_v2.6.csv`。

### 6.4 SQLite 表

新增：

- `model_training_runs`：记录模型版本、算法、训练区间、样本数、指标、特征列和模型路径。
- `model_prediction_results`：记录每只股票的次日上涨概率、方向置信度和模型方向。

## 7. 评估指标

必须输出：

- AUC。
- PR-AUC。
- 准确率。
- 分市场环境命中率。
- 分行业/板块命中率。

样本不足或单一标签时，对应指标显示为空，不伪造指标。

## 8. 页面需求

### 8.1 页面入口简化

v2.6 页面修复后，一级 Tab 从原来的大工作区拆成更贴近实盘的一屏式入口：

| Tab | 主要用途 | 一屏重点 |
|---|---|---|
| 明日计划 | 收盘后看明天关注哪几只 | A/B 核心候选、模型概率、次日验证结果 |
| 固定持仓 | 单独管理用户已有持仓 | 五只固定持仓买点、卖点、午盘、次日状态 |
| 交易记录 | 随手记录真实日内T/隔日T | 手动录入、手续费、到手利润、闭环状态 |
| 午盘验证 | 11:20 后看上午结构 | 午盘涨幅、上午结构、下午操作建议 |
| 模型训练 | 训练 v2.6 方向模型 | 样本、特征、标签、评估报告 |
| 模型预测 | 生成次日上涨概率排序 | 上涨概率、方向置信度、模型方向 |

高级工具继续折叠在页面顶部，避免干扰日常判断。

### 8.2 固定持仓

- 展示固定持仓五只股票。
- 一票一行展示买点区间、买点状态、卖点信号、卖点理由和模型概率。
- 保留午盘、次日复盘的固定持仓验证状态表。
- 行情缺失时展示待刷新状态。
- 固定持仓从「今日操作台」拆为独立 Tab，支持单独刷新固定持仓行情、刷新持仓买卖点、更新全部卖点信号。

### 8.3 明日计划

核心候选表新增：

- 次日上涨概率。
- 方向置信度。
- 模型方向。

### 8.4 交易记录与午盘验证

- 交易记录独立成 Tab，只承载真实买卖记录和闭环盈亏。
- 午盘验证独立成 Tab，只承载上午结构和下午建议。
- 人工复盘入口继续下线，真实复盘数据只从交易记录沉淀。

### 8.5 模型训练与模型预测

模型能力拆成两个 Tab：

- 模型训练：训练方向模型、查看数据集质量、查看评估报告。
- 模型预测：生成模型预测、查看次日上涨概率排序。

训练和预测拆开后，用户可以先确认样本质量，再生成预测结果，避免把“训练模型”和“当天使用预测”混在一起。

## 9. CLI

新增命令：

```bash
python main.py holdings-refresh
python main.py holdings-signals
python main.py model-train
python main.py model-predict
python main.py model-predict --stock-code 002466
```

## 10. 验收标准

- 固定持仓五只股票在卖点信号、午盘验证、次日复盘中置顶展示。
- 固定持仓行情缺失时仍显示，不直接跳过。
- `python main.py holdings-refresh` 可刷新固定持仓日线与分钟数据。
- `python main.py holdings-signals` 可刷新固定持仓买点区间、买点状态和卖点信号。
- `python main.py model-train` 可生成模型文件和评估报告。
- `python main.py model-predict` 可生成全市场候选概率排序。
- `python main.py model-predict --stock-code 002466` 可输出单只股票预测结果。
- SQLite 中存在 `model_training_runs` 和 `model_prediction_results`。
- 页面「模型训练」可以查看样本质量和评估报告。
- 页面「模型预测」可以查看概率排序。
- 页面存在独立的「固定持仓」「交易记录」「午盘验证」Tab，信息尽量在一屏中展示。

## 11. 风险

- 当前样本量较少，方向模型可能弱于规则评分。
- 行情源缺分钟数据时，固定持仓只能显示待刷新，不产生有效分时特征。
- 样本不足时评估指标不稳定，不能据此直接放大仓位。
- 方向概率只回答“收盘是否上涨”，不回答是否达到 +1% / +2%，后者留给 v2.7。
